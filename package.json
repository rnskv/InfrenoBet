{
  "name": "inferno_core",
  "version": "1.0.0",
  "description": "Игровая платформа для проведения мгновенных лотерей. ### Докер * Публикация образа",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:dev": "npm i --prefix client/ && npm i --prefix backend/ && npm i --prefix realtime/ && npm i --prefix shared/",
    "build:dev": "npm run build:dev --prefix client/ && npm run build:dev --prefix backend/ && npm run build:dev --prefix realtime",
    "pack:dev": "npm run install:dev && npm run build:dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rnskv/lottery.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rnskv/lottery/issues"
  },
  "homepage": "https://github.com/rnskv/lottery#readme"
}
